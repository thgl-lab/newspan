# 뉴스판 — PRD (Product Requirements Document)

## 0. 문서 정보

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 뉴스판 (NewsPan) |
| 한 줄 정의 | 네이버 뉴스 검색 API 기반의 개인용 뉴스 검색 웹사이트 |
| 버전 | v1.0 (MVP) |
| 작성일 | 2026-08-18 |
| 상태 | MVP 구현 완료 |

---

## 1. 프로젝트 개요

### 1.1 배경 및 목적
포털 앱이나 뉴스 앱을 켜면 광고, 추천 피드, 실시간 이슈 등 부가 정보가 많아
"내가 궁금한 키워드"만 빠르게 찾아보기가 번거롭다. 뉴스판은 검색 → 정렬 →
원문 이동이라는 최소 동선만 남긴 가벼운 뉴스 검색 사이트를 목표로 한다.

### 1.2 핵심 가치 제안
- **빠름**: 검색창 하나, 결과는 카드 리스트 하나. 클릭 수 최소화.
- **믿을 수 있는 출처**: 네이버 뉴스 검색 API의 실제 언론사 원문으로 바로 연결.
- **가벼운 개인 도구**: 로그인/저장 없이도 완결되는 1회성 검색 경험(스크랩·계정
  기능은 범위 밖, 4.4 참고).

### 1.3 타깃 사용자
- 특정 키워드(경제, 인물, 이슈 등) 뉴스를 빠르게 훑어보고 싶은 1인 사용자
- 포트폴리오 관점에서는 API 연동·상태 관리·반응형 UI 역량을 보여주는 샘플 프로젝트

---

## 2. 사용자 시나리오

| # | 시나리오 | 흐름 |
|---|---|---|
| 1 | 특정 키워드 뉴스가 궁금하다 | 검색창에 키워드 입력 → 검색 → 카드 리스트 확인 → 관심 기사 원문 클릭(새 탭) |
| 2 | 딱히 검색어가 없다 | 진입 → 인기 검색어 칩 확인 → 칩 클릭 → 바로 결과 확인 |
| 3 | 최신 소식이 궁금하다 | 검색 후 결과가 뜸 → 정렬을 "최신순"으로 전환 → 리스트 재정렬 |
| 4 | 결과가 없다 | 오탈자/희귀 키워드 검색 → "결과 없음" 화면에서 다른 키워드 유도 문구 확인 |
| 5 | API 오류가 난다 | 네트워크 문제 또는 키 미설정 상태에서 검색 → 에러 화면 확인 → "다시 시도" 클릭 |
| 6 | 모바일에서 사용한다 | 모바일 브라우저 진입 → 1열 카드 리스트로 자연스럽게 스크롤 |

---

## 3. 핵심 기능 (Feature List)

| ID | 기능 | 우선순위 | 상태 |
|---|---|---|---|
| F1 | 키워드 검색 | P0 | 완료 |
| F2 | 인기 검색어 칩 | P0 | 완료 |
| F3 | 정렬 (정확도순 / 최신순) | P0 | 완료 |
| F4 | 기사 카드 리스트 | P0 | 완료 |
| F5 | 로딩 상태 | P0 | 완료 |
| F6 | 결과 없음 상태 | P0 | 완료 |
| F7 | 에러 상태 + 재시도 | P0 | 완료 |
| F8 | 반응형 레이아웃 | P0 | 완료 |
| F9 | API 키 서버 프록시(보안) | P0 | 완료 |
| F10 | 검색 결과 개수 요약 | P1 | 완료 |
| F11 | 상대 시간 표기 (n분 전 등) | P1 | 완료 |
| F12 | 검색어 하이라이트 | P1 | 완료 |
| F13 | 실시간 인기 검색어(자체 집계) | P2 | 범위 밖 (4.4) |
| F14 | 스크랩 / 로그인 | P2 | 범위 밖 (4.4) |

---

## 4. 기술 스택

### 4.1 프레임워크 & 언어
- **Next.js 14 (App Router)** — 프론트엔드와 API 라우트를 한 프로젝트에서 처리
- **React 18**
- **JavaScript (JSX)** — 별도 상태관리 라이브러리 없이 컴포넌트 로컬 state로 처리

### 4.2 외부 API
- **네이버 뉴스 검색 API** (`openapi.naver.com/v1/search/news.json`)
  - 인증: `X-Naver-Client-Id`, `X-Naver-Client-Secret` 헤더
  - 파라미터: `query`, `display`, `start`, `sort`(`sim` | `date`)

### 4.3 스타일링
- **CSS Modules** (컴포넌트별 `.module.css`, 클래스 충돌·specificity 문제 방지)
- 전역 디자인 토큰은 `app/globals.css`에서 CSS 변수로 관리
- 웹폰트: **Pretendard**(본문, CDN), **Song Myung**(마스트헤드 로고, Google Fonts)

### 4.4 범위 밖 (Out of Scope, MVP 기준)
- 로그인 / 회원 시스템
- 기사 스크랩·북마크·읽음 상태 저장 (→ 필요 시 여기찜 방식처럼 Supabase 도입 검토)
- 실시간 인기 검색어 자체 집계 (현재는 고정 후보 목록)
- 카테고리(정치/경제/스포츠 등) 필터 — 네이버 뉴스 검색 API가 카테고리를 안 내려줌
- 페이지네이션/무한 스크롤 (현재는 1회 검색당 상위 20건만 표시)

### 4.5 보안 원칙
- `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET`은 **서버 API 라우트에서만** 사용, 브라우저로 전달 금지
- `.env.local`에 실키 보관, `.gitignore`로 커밋 제외
- `.env.local.example`만 저장소에 커밋해 필요한 값 안내
- 배포 시에는 배포 플랫폼의 환경 변수(Secret) 저장소 사용 (파일 업로드 아님)

---

## 5. 화면 구성 (IA)

단일 페이지(SPA형 구조)로 상태에 따라 콘텐츠 영역만 전환된다.

```
┌─────────────────────────────┐
│           마스트헤드            │  로고 "뉴스판" + 날짜 + 룰 라인
├─────────────────────────────┤
│           검색창              │  자동 포커스, 지우기 버튼, 검색 버튼
├─────────────────────────────┤
│         인기 검색어 칩          │  8개 고정 후보, 클릭 시 즉시 검색
├─────────────────────────────┤
│   [상태별 콘텐츠 영역 - 택1]     │
│   · 초기 안내                  │
│   · 로딩(점 애니메이션+스켈레톤)  │
│   · 결과 없음                  │
│   · 에러 + 재시도               │
│   · 결과 요약바 + 정렬 탭        │
│     + 기사 카드 리스트(1~2열)    │
├─────────────────────────────┤
│           푸터                │  출처 표기
└─────────────────────────────┘
```

### 5.1 상태 머신
```
IDLE ──(검색 실행)──▶ LOADING ──(성공, 0건)──▶ EMPTY
                        │
                        ├──(성공, 1건 이상)──▶ SUCCESS
                        │
                        └──(실패)──▶ ERROR ──(재시도)──▶ LOADING
```

---

## 6. 상세 기능 명세

### F1. 키워드 검색
- 입력창 진입 시 자동 포커스
- 빈 문자열이면 검색 버튼 비활성화
- 입력값 있을 때 X 버튼으로 즉시 초기화 가능
- Enter 또는 검색 버튼 클릭으로 제출

### F2. 인기 검색어 칩
- 고정 후보 8개(경제, 부동산, AI, 증시, 날씨, 월드컵, 환율, 선거) — `KeywordChips.jsx`의 `POPULAR_KEYWORDS` 배열에서 관리
- 클릭 시 검색창 값 세팅 + 즉시 검색 실행
- 현재 검색 중인 키워드와 일치하면 활성(선택) 스타일 표시

### F3. 정렬
- 정확도순(기본값, 네이버 `sim`) / 최신순(네이버 `date`)
- 정렬 전환 시 같은 키워드로 즉시 재검색

### F4. 기사 카드 리스트
- 카드당 표시 항목: 출처 도메인, 상대 시간, 제목(검색어 하이라이트), 요약 2줄, 원문 링크
- 카드 클릭(또는 "원문 보기") 시 새 탭에서 원문 오픈
- 1회 검색당 상위 20건 표시 (네이버 API `display=20`)

### F5. 로딩 상태
- 점 3개 바운스 애니메이션 + "기사를 찾고 있어요…" 텍스트
- 카드 자리 스켈레톤 4개 표시

### F6. 결과 없음 상태
- 검색 키워드를 인용해 안내 문구 표시
- 다른 키워드 시도 유도 (인기 검색어 칩은 화면에 계속 노출되어 있어 바로 재시도 가능)

### F7. 에러 상태
- 서버 응답의 에러 메시지를 그대로 노출 (예: 키 미설정, 인증 실패, 요청 과다, 네트워크 오류)
- "다시 시도" 버튼으로 동일 조건 재요청

### F8. 반응형
- 640px 미만: 카드 1열
- 640px 이상: 카드 2열
- 검색창/칩/결과 바는 최대 폭 제한(640~880px) + 중앙 정렬로 넓은 화면에서도 가독성 유지

### F9. API 프록시
- 클라이언트는 `/api/news?query=&sort=&display=` 형태로만 호출
- 서버 라우트가 네이버 API를 대신 호출하고 필요한 필드만 정제해 반환
- 네이버 응답 실패 시 상태 코드별로 사용자 친화적 메시지로 변환

---

## 7. 디자인 가이드

기존 프로젝트들의 "따뜻함·파스텔·둥근 모서리" 톤을 뉴스라는 소재에 맞춰
**신문 마스트헤드 + 스크랩 클리핑** 컨셉으로 재해석했다.

### 7.1 컬러
| 토큰 | 값 | 용도 |
|---|---|---|
| `--paper` | `#FBF7EE` | 배경(따뜻한 아이보리 지면) |
| `--paper-raised` | `#FFFDF8` | 카드/입력창 배경 |
| `--ink` | `#262233` | 본문 텍스트 |
| `--pine` | `#2F5D50` | 메인 액션(검색 버튼, 정렬 활성, 링크) |
| `--amber` | `#CF8A3F` | 검색어 하이라이트, 칩 hover 강조 |
| `--rose` | `#B95A63` | 에러 아이콘 |
| `--line` / `--line-strong` | `#E7DFCE` / `#D8CDB4` | 구분선, 점선 절취선 |

### 7.2 타이포그래피
- 마스트헤드/로고: **Song Myung** (손글씨 느낌의 세리프, 신문 제호 인상)
- 본문/UI: **Pretendard** (기존 프로젝트와 동일한 본문 서체 기조 유지)

### 7.3 형태 & 모션
- 모서리: 카드 14px, 칩/버튼은 완전 pill형 — 과도하게 각지지도, 과도하게 둥글지도 않게
- 시그니처 요소: 기사 카드 상단의 점선 절취선 + 종이 클립 아이콘(스크랩해둔 기사처럼)
- 인기 검색어 칩: 대시(dashed) 테두리 + hover 시 미세 회전 → 종이 태그를 만지는 듯한 촉감
- 로딩: 점 3개 바운스 애니메이션(기존 프로젝트에서 선호하는 dot-loading 패턴과 동일 기조)
- `prefers-reduced-motion` 대응: 모든 애니메이션 즉시 종료 처리

---

## 8. 제약사항 및 리스크

| 항목 | 내용 |
|---|---|
| API 요청 한도 | 네이버 오픈API 일일 호출 한도 존재 (개발자센터 등록 기준 확인 필요) |
| 인기 검색어 | 실시간이 아닌 고정 후보 — 실데이터 반영 필요 시 별도 로그 집계 설계 필요 |
| 카테고리 미제공 | 네이버 뉴스 검색 API 응답에 카테고리 필드가 없어 필터 기능 구현 불가 |
| 이미지 미제공 | 응답에 썸네일이 없어 카드가 텍스트 중심 레이아웃으로 고정됨 |
| 키 노출 리스크 | `.env.local`이 과거 커밋 이력에 포함된 적이 있다면 `.gitignore` 추가만으로는 이력에서 제거되지 않음 → 키 재발급 + 이력 정리 필요 |

---

## 9. 향후 확장 아이디어 (Post-MVP)

- 자체 검색 로그 기반 실시간 인기 검색어
- Supabase 연동 후 로그인 + 기사 스크랩 (여기찜과 동일한 아키텍처 재사용 가능)
- 검색 결과 페이지네이션 / 무한 스크롤
- 키워드별 알림(새 기사 등록 시 알림)
